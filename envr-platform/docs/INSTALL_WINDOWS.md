# Windows

Use **WSL2 Ubuntu**. Install from Microsoft Store, then follow `INSTALL_LENOVO_UBUNTU.md` inside WSL.

Native Windows without WSL: install Git for Windows, Node LTS, Python 3.12, OpenJDK, Go, and Visual Studio Build Tools for C++. Run PowerShell equivalents for disk and memory checks; shell scripts in this kit target bash.
